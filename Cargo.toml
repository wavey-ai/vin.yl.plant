[package]
name = "record-plant"
version = "0.1.0"
edition = "2021"
description = "Plant-facing vinyl manufacturing template and proof metadata for Bitneedle."
repository = "https://github.com/wavey-ai/vin.yl.plant"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
